# Работа с Git репозиторием

## 🔗 Репозиторий

**GitHub**: https://github.com/Kastrulkin/Prediction_bot

## 📝 Основные команды

### Проверка статуса
```bash
git status
```

### Добавление изменений
```bash
# Все изменения
git add .

# Конкретный файл
git add path/to/file.ts
```

### Создание коммита
```bash
git commit -m "Описание изменений"
```

### Отправка в GitHub
```bash
git push
```

### Получение изменений с GitHub
```bash
git pull
```

## 🔄 Типичный workflow

```bash
# 1. Проверить статус
git status

# 2. Добавить изменения
git add .

# 3. Закоммитить
git commit -m "Добавлена новая функция X"

# 4. Отправить на GitHub
git push
```

## ⚠️ Важно

- **НЕ коммитьте** `.env` файлы (они в `.gitignore`)
- **НЕ коммитьте** `node_modules` (они в `.gitignore`)
- **НЕ коммитьте** базы данных и бэкапы (они в `.gitignore`)
- Всегда проверяйте `git status` перед коммитом

## 🌿 Ветки (опционально)

Для работы над новой функцией:

```bash
# Создать ветку
git checkout -b feature/new-feature

# Работать над изменениями...
git add .
git commit -m "Добавлена новая функция"

# Отправить ветку
git push -u origin feature/new-feature

# Вернуться в main
git checkout main

# Слить изменения из ветки
git merge feature/new-feature
```

## 📦 Первая настройка (уже выполнено)

```bash
git init
git remote add origin https://github.com/Kastrulkin/Prediction_bot.git
git branch -M main
git add .
git commit -m "Initial commit"
git push -u origin main
```

